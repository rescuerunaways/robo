package services;


import model.Command;

public class Validator {
    public static boolean validate(Command c) {
        //if move - check if we are going to fall out
        switch (c.getName()) {
        }
        return true;
    }
}
